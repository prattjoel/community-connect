// import { AppRegistry } from 'react-native';
import Expo from 'expo';
import App from './src/App';

// AppRegistry.registerComponent('CommunityConnect', () => App);
Expo.registerRootComponent(App);
